{
  "name": "json-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "__comment1__": "json-server --watch db.json",
    "__comment2__": "json-server --watch db.json --port 4000 --routes routes.json",
    "__comment3__": "json-server --watch data.js --port 4000 --routes routes.json",
    "serve-json": "json-server --watch users.js --port 4000 --routes routes.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^6.6.6",
    "json-server": "^0.17.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0"
  }
}
